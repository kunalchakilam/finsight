Analyze my current React project and list the important React concepts, hooks, libraries, and patterns actually used in the code.

For each concept, give 2–3 simple, PPT-friendly lines explaining what it is and how we used it. Mention why we used it only where relevant (e.g., Context API for global state, Lucide React for icons).

Include things like "useState", "useEffect", Context API, routing, props, components, API calls, form handling, conditional rendering, "lucide-react", etc., only if they are actually present in the project.

Keep it concise, simple, and focused only on the React/frontend side. Don't create slides or a PPT—just give me the content/information.
